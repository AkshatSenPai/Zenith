# Frontend env. Copy to .env.local (gitignored). NEXT_PUBLIC_* are embedded in the client bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Must equal the backend ZENITH_API_TOKEN so the HUD can authenticate.
NEXT_PUBLIC_ZENITH_API_TOKEN=
