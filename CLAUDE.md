# ZENITH — Project Context for Claude Sessions
## Always read this file before starting any work

---

## WHAT IS THIS PROJECT
Full-stack personal AI assistant.
- Product name: **Zenith**
- Wake word: **"Zenith"** (simple, one word). Phase 1 activation = push-to-talk (hold space); always-listening wake-word detection is a later add. `"Hey Zenith"` is the fallback if a single word false-triggers.
- Internal codename: JARVIS (Iron Man inspired).
- Owner: freelancer + agency owner, India.
- Phase 1: personal daily driver on desktop (current goal).
- Phase 2: SaaS for Indian freelancers @ ₹999–2999/month.

Full details in JARVIS_PRD.md.

**Naming note:** "Zenith" is also used by major brands (LG's Zenith Electronics, Zenith watches/LVMH) and Indian software cos (Zenith Software, Bengaluru; Zenithra Tech, Delhi). Fine for the personal Phase-1 tool. Get a trademark lawyer's sign-off before launching the paid Phase-2 product.

**Domain (still to lock):** not zenith.com (premium ~₹1 cr). Bare "zenith" is taken/premium on most TLDs → use a compound or treatment. Leading TLD: `.app` (signals "download", free forced HTTPS) or `.in` / `.co` / `.io`. Verify live on porkbun, then trademark-check before paying.

---

## TECH STACK
- Frontend: Next.js 14 + Tailwind CSS
- Orb: **2D `<canvas>`** (`OrbCanvas.tsx`, v8 2026-07-02) — the R3F/three.js WebGL stack is REMOVED (deps uninstalled); one renderer, per-skin styles via `--orb-mode`
- Desktop shell: **Tauri** (Phase 1 — lighter than Electron)
- Backend: Python FastAPI
- AI brain: Claude Sonnet 4.6 API, via **tool use (function calling)** — NOT MCP-on-everything
- Voice in (STT): **faster-whisper**, local/offline (Phase 1). **Defaults to English (`WHISPER_LANGUAGE=en`)** for speed + accuracy — with `en` it skips transliteration. The **Hinglish path is kept but dormant**: `WHISPER_LANGUAGE=hi`/blank re-enables auto-detect + **romanise Hindi to Latin** (real words, never Devanagari/Urdu) — a Phase-2 differentiator, not deleted. **Now live on the GPU (2026-06-22): `medium`/`cuda`/`float16` on the RTX 5060** (~1.1s; `large-v3` skipped for VRAM headroom on the 8GB shared card — drop to `small`/`int8_float16` if it OOMs). A **boot-time inference warmup** (`stt_service.warm()`) avoids the one-time ~17s cuDNN init on the first call. Tunable via `.env`: `WHISPER_MODEL`, `WHISPER_DEVICE` (`cpu`/`cuda`), `WHISPER_COMPUTE`, `WHISPER_LANGUAGE`. The CUDA→CPU fallback is safe but **must be logged loudly** — it silently ran on CPU before, the real cause of the lag. (`.env` is gitignored → fresh clones run CPU until the cu128 torch + nvidia-cudnn/cublas install + the cuda keys are re-applied; see `requirements.txt`.) Cloud option for Phase 2: Deepgram (streaming)
- Voice out (TTS): **edge-tts** neural voices (Microsoft, free / no key), served from the backend at `POST /speak` as MP3 the frontend plays — browser-independent. Replaced browser SpeechSynthesis (robotic in some Chromium builds, silent in others). Voice set via `ZENITH_TTS_VOICE` (default `en-IN-NeerjaNeural` / `en-IN-PrabhatNeural`). **Kokoro** (hexgrad/Kokoro-82M) is now **implemented as a local/offline engine**, selected via `ZENITH_TTS_ENGINE=kokoro` (edge stays the fallback). English voices (default `af_heart`), returns WAV; needs the **Python 3.11** backend venv (spacy/blis have no 3.14 wheels). **Now runs on the GPU (2026-06-22): `ZENITH_KOKORO_DEVICE=cuda` (torch cu128) → ~0.7s for a paragraph vs ~17s on CPU** — the CPU path was the reply-lag AND the "PC lags when speaking" (synthesis pegged the CPU). A boot warmup (`/speak` on startup) avoids the cold-start. Kokoro's Hindi voices are weaker → English-only for now. Piper still possible. (Reply-lag now minor; TTS pre-fetch/stream is the remaining backlog — `TODO.md` §4.)
- Wake-word engine (later): Porcupine / openWakeWord, trained on "Zenith"
- Database: PostgreSQL
- Weather API (for morning briefing) — add a key, was missing from env
- Future: Anthropic Computer Use API, Higgsfield API

**Integrations are Claude tools** (not standalone MCP servers):
- Google Calendar + Gmail (multi-account) → direct Google API client libs
- Discord (multi-server) → discord.py, direct
- Notion → direct Notion REST API (internal-integration token); 18 tools; 2025-09-03 data-source model (multi-source DBs)
- WhatsApp Personal → whatsapp-mcp bridge (the ONLY place MCP is used — no official API exists)
- WhatsApp Business (multi-number) → WhatsApp Cloud API

---

## ARCHITECTURE — how it wires
- **Claude is the brain.** FastAPI defines tools; Claude decides which to call; FastAPI executes them against the real APIs and returns results. Every new integration = one more entry in `TOOLS`. The route never changes.
- **Confirm gate:** read-only tools (read calendar, read mail) run immediately; action tools (`send_email`, `send_whatsapp`, `create_event`, `delete_*`) return a "pending action" → frontend shows a confirm card → `/chat/confirm` runs it. This is how "confirm before sending/creating" is enforced.
- **Voice round-trip:** mic (MediaRecorder, hold space) → `POST /transcribe` (faster-whisper, English default) → `POST /chat` (Claude + last-20 history + rate limit + Hinglish prompt + tools + confirm gate) → reply text → `POST /speak` (TTS: local Kokoro→WAV or edge-tts→MP3) → frontend plays the audio. Reply text is rendered as markdown (emojis stripped) before display and before TTS.
- **Delivery:**
  - Phase 1 = Tauri **desktop app** (backend runs locally). No PWA.
  - Phase 2 = host the backend + ship a **PWA-installable** web app (the no-download path) + keep the Tauri desktop app as an optional download. One codebase serves all three.

---

## KEY CONSTRAINTS
- Rate limit: 5 req/min, 150 msg/day, warn at 120 — enforce a **hard daily kill-switch / budget cap**, not just a warning. Budget by tokens too (email/WhatsApp tool results balloon fast).
- Conversation history: last 20 messages.
- **English by default**; occasional Hindi words transcribe fine. Full Hinglish code-mixing kept **dormant behind the flag** for Phase 2 (code-mixing is imperfect in every STT engine — that's why it's opt-in now).
- All keys in `.env` — never hardcoded.
- Multi-account Gmail; multi-number WhatsApp Business.
- Always confirm before sending a message/email or creating/deleting events (via the confirm gate).

---

## UI STYLE
**Art direction (updated this session):** modeled on clean **app-style HUD dashboards**, NOT the dense film stills. Near-black canvas, one cyan, thin strokes, rounded "notched-corner" cards, generous spacing — legibility over decoration.
- Background #000008 · Primary #00FFE5 · Secondary #0066FF · Alert #FF6B00 · Critical #FF2020 · Text #E0F7F7
- Fonts: **Space Grotesk** (display) + **Inter** (body) + **JetBrains Mono** (chat/terminal) — now **live for the Arc skin only**, self-hosted at build via `next/font` (no runtime fetch). Ghost + Amethyst stay on the **system font stack**; a skin opts in by remapping `--font-display`/`--font-body`/`--font-mono` under its `[data-skin]` block (Arc does, in `globals.css`).
- **Center orb = 2D-canvas HUD orb (v8, 2026-07-02)** — `OrbCanvas.tsx`, a faithful port of the owner's Claude-design HUD mock (spec `docs/superpowers/specs/2026-07-02-hud-orb-port-design.md`). Per-skin styles keyed by `--orb-mode`: Arc **`sphere`** (950-pt Fibonacci particle sphere + 76-bar EQ halo + radar sweep), Ghost **`mesh`** (wobbling ink wireframe web), Amethyst **`nebula`** (1180-pt twinkling volumetric cloud + 2 rotating ellipses). All skins share: mode-driven energy, wobble, mouse tilt, ripples, **2 rotating gap rings (the old "no rings" law is fully RETIRED)**, and 4 channel lines with traveling packets wired to live connection status (chips: Gmail ↑ / Calendar → / Telegram ↓ / Discord ←). **Speaking on Arc warms to orange `255,169,77` — owner-sanctioned 2026-07-02 (overrides the old "never orange" rule; do NOT "fix" it back to cyan).** Live mic/TTS bars modulate wobble + EQ. Reduced motion freezes `t` entirely (stricter than the mock). **The R3F/three.js stack is REMOVED** (`three`, `@react-three/*` uninstalled; `OrbScene.tsx` deleted — supersedes the WebGL particle sphere AND the R3F Amethyst nebula). No WebGL contexts remain in the HUD.
- **Command center (chat):** chat input + **mic (hold space) + send merged** into one surface (`CommandCenter.tsx`) with a monospace response area (copy / save / share per answer), a **left context-rail** (chat / drafts / clients / settings), a QuickActions strip + FocusCard.
- **Panels:** left CALENDAR (`CalendarPanel`) · center orb + command center · right **Connections list** (`ConnectionsPanel` — accounts + status dots) + **Activity log** (`ActivityLog` — timestamped feed: "create_event → confirmed", "email sent", "rate-limit warning"). The old single COMMUNICATIONS panel was split into these two.
- **Confirm/pending-action card is FIRST-CLASS** — a prominent StatusCard near the orb. It's the trust layer; don't bury it.
- **CUT (do not build):** fake telemetry (CPU/GPU/disk/reactor/battery), standing weather/environment widget, decorative data-feed graph. Keep only the **real `/usage` gauge** (API usage + daily cap + token budget).
- **De-Marvel:** Zenith's own naming everywhere (your connections, your log, "Zenith" title). "JARVIS / Stark / J.A.R.V.I.S. Cloud / Pepper" never appear in the UI — JARVIS is the internal codename only.
- Top timeline/status bar · hex corner accents (kept, used sparingly). The standalone bottom waveform bar was dropped — voice activity now drives the reactive orb.
- **Skins / themes (SHIPPED 2026-06-25):** the palette is tokenized into **CSS variables** keyed by `data-skin` on `<html>` (Tailwind colors → `rgb(var(--..)/<alpha>)` so utility classes auto-theme; the orb reads its color/mode from the same vars). Three skins: **Arc** (the cyan palette above — default, unchanged, pixel-identical), **Ghost** (light **paper `#F7F7F5` + graphite ink** — NOT the old dark-mono; glow/bloom killed → depth from a hairline + whisper shadow, square corners, calmer ambient motion, **centered-focus layout** that hides the dashboard columns, the orb flips to an **ink-network web** mode (no bloom); one muted amber kept for alerts), **Amethyst** (violet `#B26BFF`; **rounded-glass** cards + **bento layout** — orb as a 2×2 hero tile, panels as tiles, a slim full-width command bar). Picked from the **Settings** view (`SkinPicker`); switch uses an emil **blur-mask crossfade** (~200ms) + a no-flash `<head>` script so there's no color flash on load. A skin = colors + treatment knobs (`--glow-strength`, `--panel-tint`, `--border-strength`, `--notch`/`--radius`, `--motion-scale`, `--orb-mode`, `--amb-grid-a`). Spec+plan in `docs/superpowers/{specs,plans}/2026-06-22-zenith-skins*.md`; built with the **Impeccable** plugin + taste / minimalist / emil. Cross-skin QA done (HUD + boot per skin, zero WebGL leak on orb mode-switch, reduced-motion honored). Deferred polish: Ghost ink-web slightly dense in the centre; Amethyst command bar centered `max-w-2xl`.

---

## BUILD ORDER (refined)
- [x] **Slice 0 — Vertical slice:** chat box + static orb + FastAPI `/chat` + one real Claude round-trip + rate-limiter stub. Prove the loop end to end before building HUD chrome.
- [x] **Milestone 1 — The Brain:** FastAPI + Claude tool-use scaffolding, last-20 history (+ token budget), enforced rate limit / kill-switch, Hinglish system prompt, confirm gate (built once, reused). Do this BEFORE integrations.
- [x] **Milestone 2 — HUD UI (✅ COMPLETE — Tauri desktop shell shipped 2026-07-13):** voice in (faster-whisper `/transcribe`) ✅ + out (Kokoro/edge `/speak`) ✅; empty/undecodable mic clips → no-speech (no 500) ✅. App-style HUD built, then given the **v7 redesign (SHIPPED to main 2026-07-03)**: IconNav + ⌘K CommandPalette + Memory/Notes/Clients views + MonthRuler + v7 boot screen + persisted reduced-motion toggle (`lib/prefs.ts`); merged **Command Center** (chat + mic + send, paginated), left/right rails, QuickActions, FocusCard, Calendar/Connections/Activity/Usage panels, top bar (`frontend/components/`). **Orb = the 2D-`<canvas>` HUD-mock port** (`OrbCanvas.tsx`, v8 — per-skin sphere/mesh/nebula; the R3F/WebGL stack was REMOVED, `OrbScene.tsx` deleted). Panels are **live** (M3+M4); markdown reply + emoji-strip ✅; three skins ✅. **Tauri desktop shell ✅ (CODE COMPLETE + live-acceptance PASSED 2026-07-13, on `feat/tauri-shell`):** Tauri v2 wraps the Next.js HUD (`output: export`) in a Win11 WebView2 desktop window (id `com.zenith.desktop`, dev port 1420); on launch the Rust host **auto-spawns the venv uvicorn backend (Option B) and kills it on exit**, a **single-instance guard** focuses the window instead of double-spawning, and the **boot screen health-gates the reveal** over the ~30–45s GPU warmup. **Mic works in WebView2** (`getUserMedia` verified; `webview2-com` permission hook). `frontend/src-tauri/` — `src/lib.rs` (spawn/kill + mic grant + single-instance), `src/backend.rs` (path-resolution + 4 cargo tests), `tauri.conf.json`; backend CORS gained the Tauri + :1420 origins. Setup + acceptance checklist in `SETUP-TAURI.md`. Gates all green: backend **260 pytest** · Rust **cargo test 4** · `tsc` · `next` static export · full `cargo build`; owner-accepted the desktop app by hand (window · boot gate · hold-Space voice loop · VRAM freed on close · single-instance). **Closes M2.**
- [x] **Milestone 3 — Google (SHIPPED 2026-06-25, live-verified):** Desktop-app OAuth (google-auth-oauthlib `InstalledAppFlow`, client id/secret from `.env` — no `client_secret.json`; connect runs in a background thread, frontend polls `/google/status`), least-privilege scopes (`calendar.events`/`gmail.readonly`/`gmail.send`), per-email tokens in gitignored `backend/tokens/` (multi-account-ready). **11 tools on the EXISTING loop + confirm gate, zero route/gate changes** — Calendar get/search/create/update/delete, Gmail get/search/read/send, `get_weather`, `get_briefing`; `create_event`/`update_event`/`delete_event`/`send_email` are gated. Live Connections/Calendar panels + orb Gmail/Calendar nodes via `/google/status` + `/calendar/events` (share the service layer, no API tokens spent; WhatsApp/Discord stay mock). "Good morning" button → briefing through the loop, spoken. **Live-verified by the owner 2026-06-25:** connect → calendar read → schedule a call (confirm→created) → send email (confirm→sent) → spoken briefing, all working. A **real Activity Log** (`backend/activity_log.py` → `GET /activity`, in-memory, records each successful tool run; replaces the mock) and the Connections connected-row polish landed right after. Setup: `SETUP-GOOGLE.md`. **ActivityLog is now live (not mock); the only mock left in the HUD is WhatsApp/Discord connection status — M4.**
- [x] **Milestone 4 — Messaging (SHIPPED 2026-06-25 — Discord + Telegram both live-verified):** **Discord done first** as 4 tools on the EXISTING loop + confirm gate (zero route/gate changes) — `list_discord_channels`/`get_discord_messages`/`search_discord_messages` (read-only) + `send_discord_message` (gated). **discord.py** gateway bot runs as a background task on FastAPI's event loop; the sync `run_tool` reaches it via `asyncio.run_coroutine_threadsafe` (`backend/discord_service.py`, `_bridge` takes a coro factory). **Server channels ONLY — never DMs (a user-token would be a ToS/ban risk).** Needs the **Message Content** intent + `DISCORD_BOT_TOKEN` in `.env` (`SETUP-DISCORD.md`). `/discord/status` lights the orb Discord node + Connections row (token-based, no connect button); tool runs hit the live Activity Log. Backend unit tests (7) + HUD verified, then **live-verified by the owner 2026-06-25** (bot on their server reads channel messages + posts messages). **PLUS a Telegram remote (BUILT 2026-06-25):** a phone front-end into the EXISTING brain — **NOT a tool**. The chat loop was extracted from `main.py` into **`chat_core.py`** (`process_chat`/`process_confirm`, shared by the HUD `/chat` AND the bot — never forked); **`memory_service` is now per-channel** (HUD vs telegram = separate last-20, shared rate limiter). `backend/telegram_service.py`: python-telegram-bot v20 **long-polling** on the FastAPI loop, async handlers bridge to the sync core via `asyncio.to_thread`; **LOCKED to `TELEGRAM_ALLOWED_USER_IDS`** (fail-closed — empty list rejects everyone; others ignored+logged); the confirm gate renders as `[Confirm][Cancel]` **inline buttons** → `process_confirm`. `/telegram/status` + the orb's **WhatsApp node became Telegram** (`SETUP-TELEGRAM.md`). **WhatsApp-personal is PARKED** (unofficial bridge = ban risk; parked, not deleted — Telegram took its orb slot); **WhatsApp Business stays Phase-2.** Telegram unit tests (4) + HUD node screenshot verified, then **✅ SHIPPED — live-verified by the owner 2026-06-25** (phone → reply; "email Rahul" → `[Confirm]` button → actually sends; reads mail/calendar via the Telegram ingress). **Both Discord + Telegram are live-verified, shipped, and pushed to `origin/main`.**
- [x] **Milestone 5 — Hardening (✅ SHIPPED 2026-06-26 — both parts merged to main):** **Security part 1 done (`SECURITY.md`):** backend API token (`X-Zenith-Token` on every route except `/` + `/health`; `backend/auth.py`; fail-open+warn when unset / 401 when set) + one frontend `apiFetch` wrapper (`lib/api.ts`); prompt-injection guard (fence untrusted read-tool results as `<external-content>` + a system-prompt rule + a same-turn ⚠️ warning on the HUD confirm card AND Telegram buttons; gate still never auto-approves); secrets restricted at rest (`secure_files.harden()` — `icacls`/`chmod 600` on `.env` + `tokens/` at boot); thread-safe rate limiter verified (+ concurrency tests); logs scrubbed behind `ZENITH_DEBUG_LOGS`; fixed a `.gitignore` inline-comment bug that left `backend/tokens/` (live OAuth tokens) un-ignored (never committed). **+23 tests; live-verified** (auth 401/200, real `/chat`, HUD all-200). Telegram/Discord unaffected (they call `chat_core` in-process). Encryption-at-rest deferred to Phase-2. **Part 2 ✅ (commit 4d68ff5, merged to main):** real usage/cost dashboard (`UsagePanel` — token in/out split → est. ₹/$ + kill-switch chip, all 3 skins), Settings page (`SettingsView` from `/health` v0.5.0 + config block), README + `.env.example` rewrite, error/empty/loading audit (Retry states on Activity/Calendar/Usage + Connections live/offline chip + stale `demo` badge removed; verified live backend-down + auto-recovery), +18 tests (82 fast suite green). **M5 done.**

### Phase-1 capabilities pulled in from "future" (this session's call: it's a daily driver for ME, so the things I'd actually use live in Phase 1, not parked as Phase-2 product ambition)
- [x] **Milestone 6 — Memory vault + Copy Factory (✅ SHIPPED — both parts merged to main; vault 2026-06-26, Copy Factory 2026-06-27):**
  - **Memory = local Markdown vault ✅ (Obsidian-style), NOT Postgres** — `backend/vault_service.py` (all filesystem work + **path-safety**: no `..`/absolute/drive escape, final-path-under-root check) on the EXISTING loop. 4 tools: `search_notes`/`read_note`/`list_notes` (read, **trusted** — owner's own notes, not fenced) + `save_note` (**local write, NOT gated** — runs immediately; the gate is for external/destructive only). Daily log = `folder='daily'`, empty title → `daily/<today YYYY-MM-DD>.md` appended a timestamped line (shares the Obsidian Daily Note). Path from `ZENITH_VAULT_PATH` (default `backend/vault`, gitignored; **points at the owner's real Obsidian vault `C:\Users\Akshat Singh\ZenithVault`**). HUD: read-only `VaultView` on the rail's Drafts (recent) / Clients (`clients/`) tabs via `/vault/notes`+`/vault/note` (non-tool routes). +18 tests; live-verified end to end ("promised Rahul…"→daily log, recall, client note, `../escape` rejected). Plan: `docs/superpowers/plans/2026-06-26-memory-vault.md`.
  - **Copy Factory / Template Studio — Part 2 ✅ (SHIPPED 2026-06-27, live-verified, merged to main):** 3 **output-only** tools on the EXISTING loop (NOT in `ACTION_TOOLS`, never gated; reads are trusted, nothing wired to Arkquen) — `draft_sequence` (coherent multi-stage email + **Meta WABA-template** WhatsApp journey: Enquiry→Nurture→Booking→Reminder→No-show, `{{1}}` positional vars, category-tagged), `draft_ad_copy` (Meta primary/headlines + creative brief OR Google RSA within char limits), `draft_landing_copy` (hero→trust→benefits→FAQ→final CTA), in English/Hindi/Hinglish (Latin only). New module `backend/copy_factory.py` is a thin **brief-assembler**: resolves the brief from `clients/<name>.md` (or inline text) + the owner's voice from `notes/voice.md`/recent client notes, returns a **directive** (brief + voice + exact format) as the tool result; the loop's Claude then writes the finished copy into the Command Center (copy/save already exist), saved to the vault via the existing `save_note` → shows in the Drafts tab. **Copy only — I paste into Arkquen; nothing sent.** `MAX_TOKENS` 1024→8192 (non-streaming-safe) so a full sequence isn't truncated. +9 tests (111 fast suite green); live-verified. Plan: `docs/superpowers/plans/2026-06-26-copy-factory.md`.
  - **To-dos ✅ (SHIPPED 2026-06-28):** a vault-backed to-do list on the EXISTING loop — `backend/todo_service.py` owns one Obsidian-style checklist file `Todos.md` at the vault root (`- [ ]`/`- [x]`; non-checklist lines preserved; a to-do = 0-based index among checklist lines; reuses `vault_service.vault_root()`). 3 tools `add_todo`/`list_todos`/`complete_todo` (**local writes, NOT in `ACTION_TOOLS` (never gated), NOT in `UNTRUSTED_TOOLS`** — same class as `save_note`; added to `activity_log._MAP` as type `note`). Editable **Today's Focus** HUD card (`FocusCard`) via REST `GET/POST/PATCH/DELETE /todos` (behind the auth gate, shares `todo_service`), re-fetches on window focus so voice-added items appear; removed the dead `focus` mock. The two stale M6 QuickActions (Draft proposal, Log note) now prefill the Command Center. +12 tests (123 fast suite green); live-verified. Plan: `docs/superpowers/plans/2026-06-27-todos.md`.
- [x] **Notion integration (✅ SHIPPED 2026-07-05, live-verified — merged to main):** **18 tools on the EXISTING loop + confirm gate** (direct Notion REST API, NOT MCP; zero route/gate changes) — reads `list_notion_pages`/`list_notion_databases`/`search_notion`/`read_notion_page`/`query_notion_database`/`describe_notion_database`/`get_notion_comments` (Notion content fenced as `<external-content>`) + gated writes `create_notion_page` (rich blocks) / `create_notion_database_item` / `append_to_notion_page` / `update_notion_page` / `archive_notion_page` (soft-delete → trash) / `update_notion_block` / `delete_notion_block` (find a line by text) / `create_notion_database` / `update_notion_database` / `add_notion_comment`. New `backend/notion_service.py`. **Migrated to Notion's 2025-09-03 data-source model** so multi-data-source DBs work (routed via `_db_schema`/`_db_query`/`_db_parent`/`_data_source_id`); live QA fixed 3 undocumented quirks — `/search` filters by `"data_source"`, column edits `PATCH /data_sources/{id}` (DB-level PATCH no-ops), search/list ids are *data-source* ids vs URL *database* ids. Auth = internal-integration `NOTION_API_KEY`; pages/DBs must be **shared with the integration** in Notion; comment tools need the Read/Insert-comments capability. Setup `SETUP-NOTION.md`. All 18 tools live-verified; **161 backend tests green, tsc clean.** Rode along: HUD **speaks confirmed-action results** (was silent) + daily token cap **300k→500k** (env `ZENITH_DAILY_TOKEN_BUDGET`).
- [x] **App Launcher (✅ SHIPPED 2026-07-09, live-verified):** open the owner's pre-approved apps / files / folders / websites by voice or text — 2 tools on the EXISTING loop, zero route changes. **Security model = WHITELIST ONLY:** the tool argument is always a *name to look up*, never a path or command to execute; there is deliberately **no "run any command" escape hatch**. `backend/app_launcher.py` reads `backend/apps.json` (gitignored personal list; `apps.example.json` is the committed template + fallback) **fresh on every call**, so edits need no restart; `ZENITH_APPS_PATH` overrides the location. Matching is stdlib-only (normalize → exact name/alias → substring → `difflib` fuzzy) and **refuses + lists the options rather than guessing** when a name is unknown or ambiguous. Five target kinds: `url` (webbrowser) · `path` (ShellExecute — exe/folder/`.lnk`) · `protocol` · `command` (`shutil.which`) · `uwp` (Store apps via `explorer.exe shell:AppsFolder\<AppUserModelID>`). `open_app` is **NOT in `ACTION_TOOLS`** (runs immediately) but IS in the new **`GATE_IF_UNTRUSTED`** set — it becomes a confirm-gated pending action when the same turn already pulled in untrusted `<external-content>`, so a prompt-injected "open X" inside an email can't auto-launch. That reuses `run_loop`'s existing `saw_untrusted` flag; the gate itself is unchanged. `list_apps` is read-only and unlogged; `open_app` records to the Activity Log as type `note`. **OUT OF SCOPE by design:** Computer Use / screenshots / clicking / typing, arbitrary commands or shell access, and closing/killing apps or window management. Setup `SETUP-APPS.md`; spec + plan in `docs/superpowers/{specs,plans}/2026-07-08-app-launcher*.md`. +34 tests (197 fast suite green); live-verified across all five target kinds.
- [~] **Milestone 7 — Proactivity + message triage (Part 1 App Launcher ✅ · Part 2 Proactivity ✅ SHIPPED 2026-07-10 · Part 3 triage TODO):**
  - **Part 2 — Proactive nudges ✅ (SHIPPED 2026-07-10, live-verified, merged to main):** surfaces *what slipped on your side* as **≤3 dismissable/snoozable nudge cards** above the Command Center, computed **on demand** (60s poll + window focus — no background loop; Phase 1 has no push channel until Tauri). New `backend/proactivity_service.py` + 2 routes (`GET /proactive`, `POST /proactive/dismiss`), zero changes to the loop or the confirm gate. **Two gatherers:** Calendar (deterministic — `prep` for a meeting within 45 min, `deadline` for an all-day item due today/tomorrow) and **commitments** (Claude extracts `{what, who, by_when, done}` from the last 7 days of vault `daily/*.md`). **Sources are trusted-only** (the owner's own calendar + notes) — inbound-message reading is Part 3, deliberately. **Two hard safety guarantees:** the extraction call binds **NO tools** (structurally can only return JSON, never act — asserted in a test), and a nudge's action is a **prefill string that never auto-runs** — anything that sends/creates still hits the existing confirm gate. Extraction is **cached on a signature of the daily-note window** (names+mtimes+sizes) so Claude fires a few times a day, not every poll; it calls `limiter.ensure_budget()` and is **skipped when the kill-switch is tripped** (calendar nudges still work), records usage, and is exempt from the 5/min *request* limit (it isn't a user turn). Dismiss/snooze persist in gitignored `backend/.zenith/proactive.json` (same atomic tmp→`os.replace` pattern as `history.json`); a **stable `kind:slug:shorthash` id** means dismiss/snooze remember *that* item across recomputes while a materially-changed subject re-surfaces. Every gatherer is best-effort — one throwing contributes nothing and the endpoint never 500s. HUD: `NudgeStack` + `NudgeCard` reuse StatusCard's notched shell via a new shared `cardShell.tsx` (Corner/TONES extracted; StatusCard renders identically), so all three skins auto-theme with zero per-skin work. **An active confirm card always renders above the nudges** — the trust layer outranks proactivity. Spec+plan in `docs/superpowers/{specs,plans}/2026-07-09-proactivity*.md`. **+22 tests (221 backend green), tsc clean; live-verified on Arc/Ghost/Amethyst** (real Claude extraction: an open commitment surfaced, a logged-as-done one auto-cleared, a non-commitment "just an idea" line correctly ignored; dismiss + snooze survive a reload; the action button prefills without sending).
  - **Part 3 — Gmail message triage ✅ (SHIPPED 2026-07-10, live-verified, merged to main):** "who's waiting on a reply from me?" as a dedicated **Triage view** (new TRIAGE entry in IconNav) + a chat tool. **Gmail only** in v1 (WhatsApp parked; Discord deferred — its bot never sees DMs and has no owner-identity). New `backend/triage_service.py` finds waiting threads **deterministically, zero Claude tokens**: query `in:inbox category:primary newer_than:14d`, then per thread skip if the *last* message is from me / a `noreply` sender / bulk mail / younger than 4h; rank oldest-first, cap 10. Four new `google_service` helpers (`me_address`, `list_thread_ids`, `thread_summary`, `reply_to_thread`). **Two tools on the EXISTING loop + confirm gate, zero route/gate changes:** `list_waiting_replies` (read-only, **in `UNTRUSTED_TOOLS`** → fenced as `<external-content>`) and `reply_email(thread_id, body)` (**in `ACTION_TOOLS`** → always confirm-gated). **Security invariant: `reply_email` exposes NO recipient — To/Subject/In-Reply-To/References are derived server-side from the thread's last message, so a prompt-injected email can't redirect a reply.** `GET /triage` feeds `TriageView`; the **Draft reply** button prefills the Command Center (inert string, never runs a tool) and switches to chat. **Bulk-mail filter** (`List-Unsubscribe`/`List-Id`/`Precedence: bulk|list`) drops newsletters/notifications for zero tokens — live QA cut 25 candidates to 5. Setup: no new OAuth scope (M3 already granted `gmail.readonly`+`gmail.send`). Spec+plan `docs/superpowers/{specs,plans}/2026-07-10-message-triage*.md`. **+34 triage tests (255 backend green), tsc clean, live-verified on the real inbox across Arc/Ghost/Amethyst.** **M7 COMPLETE.** Deferred to a Part-3.1 follow-up: a **Claude-classification pass** for the residual transactional noise (bank alerts/receipts that carry no `List-Unsubscribe`); Discord/WhatsApp triage; multi-account.

> **Stays in Phase 2 (true SaaS-only):** multi-user auth (Clerk), per-user encrypted keys, Razorpay billing, hosted backend + PWA, the full **business-data dashboard** (clients/projects/invoices/time as a DB module — the lightweight version is just the vault in M6). Computer Use + Higgsfield remain optional/experimental.

---

## KEY DECISIONS & GOTCHAS
- **STT:** Web Speech API dropped — recognition breaks inside the desktop shell (it depends on Chrome → Google's servers). Use faster-whisper locally instead.
- **STT language (decided this session):** **default to English (`WHISPER_LANGUAGE=en`)** for speed + accuracy. The Hinglish path — auto-detect + **romanise Hindi to Latin** (not translated; re-force off Urdu), `beam_size=5` + `condition_on_previous_text=False`, `vad_filter` (fixed a ~58s silent-decode) — is **kept but dormant behind the flag** (Phase-2 differentiator; do NOT delete). With `en`, transliteration is skipped entirely.
- **faster-whisper:** load the model ONCE at startup, not per request. **`large-v3` on `cuda`/`float16` on the 32GB desktop GPU** (~2-3s for a 12s clip); `small`/`medium` on the 8GB MacBook. **The safe CUDA→CPU fallback was silently running on CPU even on the GPU box — the real cause of the ~20s latency.** Make it LOUD: log requested-vs-actual device/model at startup, warn on CUDA-unavailable (likely missing CUDA 12 + cuDNN — `nvidia-cublas-cu12` / `nvidia-cudnn-cu12`), expose active device/model on `/usage` (or a new `/health`). NVIDIA only. Push-to-talk masks remaining latency.
- **TTS (decided Pass B):** browser SpeechSynthesis replaced with **edge-tts** neural voices (free, no key) — backend renders MP3 at `POST /speak`, frontend plays it. Browser-independent; not rate-limited. New deps: `edge-tts`, `indic-transliteration`.
- **Replies:** rendered as markdown (bold/lists/code), **emojis stripped** on display and before TTS. The system prompt forbids emojis + heavy formatting and forces Latin-script Hinglish (never Devanagari/Urdu).
- **Tauri:** grant mic permission in the Tauri config + OS-level usage string, or `getUserMedia` fails silently.
- **WhatsApp personal (bridge):** unofficial → ToS/ban risk. Don't point it at a number you can't afford to lose.
- **WhatsApp Business "1000 free msgs":** outdated — verify Meta's current per-conversation pricing; ship with ONE business number first.
- **Domain:** still to lock (non-.com). Verify on porkbun, then trademark-check before paying.
- **Differentiation / moat:** the field splits into pretty JARVIS clones (great UI, no real work) and serious assistants (powerful, no soul). Zenith's lane → *a proactive, Hinglish-speaking, WhatsApp-native assistant that handles the founder's work and acts with a visible trust layer (confirm gate + activity log).* Aesthetics get attention; that sentence is the reason to pay.
- **DON'T BUILD (saturated / non-differentiating — zero effort):** PC/system monitoring (CPU/GPU/disk/reactor/battery telemetry — cosplay); calendar auto-scheduling à la Motion (just integrate Calendar); WhatsApp Business customer-chatbots / lead-gen (different product, saturated in India); smart-home/IoT; more 3D holographic eye-candy.
- **Arkquen = OUT of Zenith's scope.** It's a white-label subscription being rebuilt — a moving target. Arkquen runs the *client machine* (funnels/CRM/automated sequences); Zenith runs the *founder* (proposals, ad copy, briefs, reporting, comms, Copy Factory). No integration.
- **Memory = Markdown vault** (Obsidian-style), not Postgres — local, private, and it doubles as the Copy Factory's brief store (one client brief → sequences + ad copy + proposals + pre-call briefs).
- **App Launcher = whitelist ONLY.** Zenith may only launch entries in `backend/apps.json`. It must **never** run an arbitrary path/command a caller — or a prompt-injected email — supplies. That *is* the security model: do **not** add a "run any command" escape hatch, and don't relax the never-guess rule (unknown/ambiguous name → refuse + list). `open_app` runs immediately on a normal turn but is auto-gated via `GATE_IF_UNTRUSTED` when untrusted content was read in the same turn. Store apps launch by AppUserModelID (`type: "uwp"`, find it with `Get-StartApps`); everything else installed is easiest as its Start-Menu `.lnk` with `type: "path"`.
- **Triage `reply_email` = thread-derived recipient, always gated.** The model supplies **only `body`**; `To`/`Subject`/`In-Reply-To`/`References` come from the thread's last message (`google_service.reply_to_thread`). The tool schema deliberately has **no `to` property** — a test asserts `set(props) == {"thread_id","body"}`, so a future edit that adds a recipient fails CI. So the worst a prompt-injected email can do is make Zenith reply *to the person who already emailed you*, never to an attacker's address. `reply_email` is in `ACTION_TOOLS` (always confirm-gated); `list_waiting_replies` is in `UNTRUSTED_TOOLS` (fenced). Detection is deterministic/zero-token (a thread waits iff its **last** message isn't mine, isn't `noreply`, isn't bulk, and is >4h old) — do NOT add a `-from:me` operator to the Gmail query (`threads.list` matches a thread if *any* message matches, so it filters nothing; the last-message check is authoritative). **Bulk filter = `List-Unsubscribe`/`List-Id`/`Precedence: bulk|list`** — these are set by all marketing/list mail and never by person-to-person mail, so it's high-precision and zero-token; keep it. Residual transactional noise (bank alerts/receipts) is the deferred Claude-classification pass's job. The Triage view is **pull-only** (never a nudge) precisely because its content is attacker-controlled; `getTriage` uses `cache: "no-store"` so the list always reflects the inbox now.
- **Proactivity = trusted sources + inert actions.** Two invariants hold the whole feature up; do **not** relax either. (1) The commitment-extraction Claude call binds **no tools** — it can only return JSON, so even a poisoned daily note (pasted external text) cannot make it act. There is a test asserting `"tools" not in` the call kwargs; don't "helpfully" add tools to give it context. (2) A nudge's `action` is a **prefill string only** — it drops text into the Command Center and rides the normal loop + confirm gate. Never wire a nudge button straight to a tool. Sources are the owner's own **Calendar + vault daily notes** (trusted, not fenced); reading inbound messages is M7 Part 3, where the `<external-content>` fencing lives. Nudges are **on-demand** (60s poll + focus), not a background loop — Phase 1 has no push channel, so a loop's nudges would sit unseen until the HUD opens; revisit once Tauri notifications exist. Extraction is cached on a daily-note signature and **skipped when the token kill-switch trips**. Anti-nag (≤3 cards, dismiss/snooze, auto-clear on `done`) is a first-class design goal, not polish — a proactive assistant that nags gets muted in a day.
- **Orb (v8, 2026-07-02 — 2D-canvas port of the owner's HUD mock; supersedes v1.5 R3F sphere + the v7 R3F nebula):** `OrbCanvas.tsx` is the ONLY orb renderer — a faithful port of `_initOrb` from the owner's Claude-design "Zenith HUD.html". Per-skin styles via `--orb-mode`: Arc `sphere` (Fibonacci particle sphere + EQ halo + radar sweep) / Ghost `mesh` (ink wireframe web) / Amethyst `nebula` (volumetric cloud + rotating ellipses); all share energy/wobble/tilt/ripples/**gap rings (ring law RETIRED)**/channel lines with packets driven by live connection status. **Orange-on-speak (`255,169,77`) on the Arc sphere is owner-sanctioned — do NOT revert to cyan.** Live audio bars fold into wobble+EQ; reduced motion freezes `t`. **R3F/three deps REMOVED** (`three`, `@react-three/*`; `OrbScene.tsx` deleted) — no WebGL anywhere, so the old dispose-on-view-switch leak class is gone. Spec: `docs/superpowers/specs/2026-07-02-hud-orb-port-design.md`.
- **TTS (v1.6 — SHIPPED):** **Kokoro is now the local/offline engine** (`ZENITH_TTS_ENGINE=kokoro`, default voice `af_heart`, English; edge-tts kept as the one-flag fallback). `synthesize()` dispatches by engine and returns `(bytes, media_type)` — edge→MP3, Kokoro→WAV — so `/speak` + the frontend serve either unchanged. Needs the **Python 3.11** backend venv (spacy/blis lack 3.14 wheels); the old 3.14 venv is parked as `backend/.venv314-bak`. **now on GPU** via `ZENITH_KOKORO_DEVICE=cuda` (~0.7s paragraph; see v1.7 below). `/health` reports the active engine/voice. Kokoro Hindi is weak → English-only for now. Reply-lag now minor on GPU — pre-fetch/stream is **TODO §4**.
- **Voice → GPU (v1.7 — SHIPPED 2026-06-22):** both Kokoro TTS and Whisper STT now run on the RTX 5060 (torch reinstalled as **cu128** for Blackwell sm_120; whisper `medium`/`cuda`/`float16` via `nvidia-cudnn-cu12`/`nvidia-cublas-cu12`). Paragraph TTS **17.6s → ~1.3s**, STT ~1.1s, CPU freed — killed both the reply-lag and "PC lags when speaking" (CPU synth was pegging the cores). Both engines get a **boot-time inference warmup** (`stt_service.warm()` + a `/speak` warmup in `main`) so the first call isn't a ~17-30s cuDNN cold-start. Device is `.env`-driven (gitignored → fresh clones run CPU); code default stays CPU. 8GB card is shared → `medium` not `large-v3`; `/health` shows `device==cuda fallback==false`. Backend boot now ~30-45s (warmups).
- **Skins (v1.8 — SHIPPED 2026-06-25):** the palette is tokenized into CSS vars keyed by `data-skin`; three skins — **Arc** (cyan default, unchanged) / **Ghost** (light **paper + ink**, centered-focus layout, **ink-network** orb) / **Amethyst** (violet, rounded-glass **bento** layout). Picked from Settings (`SkinPicker`); switch = emil blur-mask crossfade + no-flash load. Tasks 1–10 done, each an atomic commit on `main` (`9e3b7b7..bc2a3f2`); cross-skin QA passed (HUD+boot screenshots, no WebGL leak on orb sphere↔network switch, reduced-motion honored). Spec+plan in `docs/superpowers/{specs,plans}/2026-06-22-zenith-skins*.md`; built with the **Impeccable** plugin + taste / minimalist / emil. (See UI STYLE.)

---

## BUSINESS CONTEXT (the owner's world — what Zenith is built around)
- **Arkquen** (arkquen.com) — real-estate-focused funnel / CRM / WhatsApp+email automation platform. Currently a **white-labeled subscription the owner resells**, on the way to **building their own**. Runs the *client-facing machine*. **Zenith does NOT integrate with it.**
- **ShapeOdyssey** (shapeodyssey.com) — digital agency that builds **customer-acquisition systems**: Meta/Google ads + funnels (in Arkquen) + websites + automation. Team-based.
- **Division of labour:** Arkquen runs the client machine → **Zenith runs *you***: proposals, agreements, ad copy, creative briefs, campaign reporting, website/funnel copy, ad-hoc client comms, and the Copy Factory. None of it overlaps Arkquen.
- **Long-game (Phase 2):** the owner works in a team, so Zenith eventually trends toward a *founder's command center over a team* (delegation, "what's pending across everyone"). Phase 1 stays the personal driver.

---

## COST STRUCTURE (Phase 1, updated)
| Service | Monthly Cost |
|---|---|
| Claude Pro (for building) | ~₹1,680 |
| Anthropic API — Sonnet 4.6 | ~₹420 |
| faster-whisper STT | Free (runs locally) |
| Google / Discord / WhatsApp personal | Free |
| **Total MVP** | **~₹2,100/month** |

---

## MASTER PROMPT (updated short version)
Build **Zenith** — a full-stack personal AI desktop assistant (wake word "Zenith", codename JARVIS).
Stack: Next.js + Tauri desktop shell + Python FastAPI + Claude Sonnet 4.6 (tool use) + faster-whisper (local STT, **English default**) + neural TTS (local **Kokoro** default / edge-tts fallback, backend `/speak`) + a 2D-canvas HUD orb (per-skin sphere/mesh/nebula) + PostgreSQL.
Integrations as Claude tools: Google Calendar/Gmail (multi-account, direct API), Discord (discord.py), WhatsApp personal (whatsapp-mcp bridge), WhatsApp Business (Cloud API, multi-number).
Architecture: Claude calls tools, FastAPI executes them; read-only tools run immediately, action tools (send/create/delete) go through a confirm gate. Voice loop: mic → /transcribe (whisper) → /chat (Claude + last-20 history + rate limit + Hinglish prompt + tools + confirm) → /speak (Kokoro local / edge-tts fallback).
UI: app-style HUD — #000008 bg, #00FFE5 cyan, **2D-canvas HUD orb (per-skin sphere/mesh/nebula, audio-reactive, gap rings, orange-on-speak on Arc)**, connections list, activity log, first-class confirm card, real /usage gauge, merged Command Center.
Constraints: hard 5/min + 150/day cap, last 20 msgs, English default (Hinglish dormant behind flag), .env keys only, confirm before actions.
Production quality, complete code, no placeholders. Deliver: full codebase + README + .env.example.

---

*Synced with JARVIS_PRD.md v2.8. v2.8 (Tauri desktop shell — **M2 close-out**, CODE COMPLETE + live-acceptance PASSED 2026-07-13, on `feat/tauri-shell` `e2232ae..777f92c`, this doc commit rides the merge to main): the last piece of Milestone 2 — the existing Zenith HUD now runs as a **native Windows desktop app (Tauri v2)**. Tauri wraps the Next.js HUD as a **static export** (`output: "export"` → `frontend/out`) rendered in Win11's **WebView2**; `frontend/src-tauri/` (Rust host, id `com.zenith.desktop`, dev port 1420). On launch the Rust host **auto-spawns the owner's venv uvicorn backend (Option B)** — `<python> -m uvicorn main:app --host 127.0.0.1 --port 8000`, `CREATE_NO_WINDOW`, paths from `ZENITH_BACKEND_DIR`/`ZENITH_PYTHON` — and **kills only a backend it spawned** on exit (probes :8000 first; if already served, it neither spawns nor kills). A **single-instance guard** (`tauri-plugin-single-instance`, registered first) focuses the existing window instead of double-spawning. The **`BootScreen` health-gates the reveal** (`isTauri()` → polls `/health` every 1s up to 90s, shows "STARTING BACKEND…") so the ~30–45s GPU warmup doesn't flash a false "offline". **Mic works in WebView2** — `getUserMedia({audio:true})` verified; a `webview2-com` (0.38) `PermissionRequested` hook auto-grants `COREWEBVIEW2_PERMISSION_KIND_MICROPHONE` (added proactively, not probe-gated — no GUI to probe). Backend CORS default gained `http://tauri.localhost`/`https://tauri.localhost`/`tauri://localhost` + `:1420` (extracted `allowed_origins()`, `test_cors.py`). **The backend, chat loop, confirm gate, and voice endpoints are UNCHANGED** — only `main.py` CORS defaults + a `next.config.mjs` `output: export` + the new `src-tauri/` shell. Backend spawn/kill/path-resolution live in `src/lib.rs` + `src/backend.rs` (4 cargo tests: env-vs-default backend dir + venv python resolution). Executed **inline** as 8 atomic commits (Tasks 1–8; the mic Rust hook was folded in without a probe since there's no headless way to probe a GUI). Spec+plan `docs/superpowers/{specs,plans}/2026-07-12-tauri-shell*.md`; run/build + acceptance in `SETUP-TAURI.md`. **Gates ALL green: backend 260 pytest · Rust `cargo test` 4 · `tsc` clean · `next` static export · full `cargo build`.** **Owner live-accepted the desktop app by hand** — native window renders the HUD, boot gate holds then reveals on `/health` green, **hold-Space voice loop works end-to-end (mic → /transcribe → /chat → /speak)**, VRAM freed on close, second launch focuses (one uvicorn). **Milestone 2 COMPLETE.** Next: **wake word "Zenith"** (the marquee Phase-1 capability the shell unblocks), then a background proactivity watcher + native notifications, system tray, global push-to-talk hotkey, autostart. v2.7 (Gmail message triage — M7 Part 3, SHIPPED 2026-07-10, `feat/message-triage` ff-merged to main `05415f5..a8b9c1a` + pushed to origin): **"who's waiting on a reply?"** as a dedicated **Triage view** (new TRIAGE IconNav entry) + a chat tool `list_waiting_replies`, plus a gated `reply_email`. **Gmail only** (WhatsApp parked; Discord deferred). New `backend/triage_service.py` finds waiting threads **deterministically, zero Claude tokens** (query `in:inbox category:primary newer_than:14d`, then per-thread: last message not mine, not `noreply`, not bulk, >4h old; rank oldest-first, cap 10). Two tools on the EXISTING loop+gate, zero route/gate changes: `list_waiting_replies` (read-only, **`UNTRUSTED_TOOLS`** → fenced) + `reply_email(thread_id, body)` (**`ACTION_TOOLS`** → always gated). **Security invariant: `reply_email` takes NO recipient — To/Subject/threading are derived server-side from the thread's last message**, so a prompt-injected email can't redirect a reply (a test asserts the schema is exactly `{thread_id, body}`). `GET /triage` → `TriageView`; **Draft reply** prefills the Command Center (inert, never runs a tool) and switches to chat. Four new `google_service` helpers; no new OAuth scope. Executed via subagent-driven-development (Tasks 1-4) then inline (Tasks 5-8, after a subagent session-cap); every task TDD + reviewed. **Two live-QA-driven, individually-revertible additions under owner autonomy:** a **bulk-mail filter** (`List-Unsubscribe`/`List-Id`/`Precedence`, cut 25 live candidates → 5, all newsletters gone; `5bd705a`) and `getTriage` **`cache: "no-store"`** (`a8b9c1a`, a stale-list fix). Spec+plan `docs/superpowers/{specs,plans}/2026-07-10-message-triage*.md`. **+34 triage tests (255 backend green), tsc clean, live-verified on the real inbox across all 3 skins. M7 COMPLETE.** Deferred to Part-3.1: a Claude-classification pass for residual transactional noise (bank alerts/receipts with no `List-Unsubscribe`), Discord/WhatsApp triage, multi-account. v2.6 (Proactivity — M7 Part 2, SHIPPED 2026-07-10, `feat/proactivity` ff-merged to main `ea4bd0c..b4ba17a`): **proactive nudges** — ≤3 dismissable/snoozable cards above the Command Center surfacing approaching meetings, all-day deadlines, and unkept commitments extracted from the vault's daily notes. New `backend/proactivity_service.py` + `GET /proactive` / `POST /proactive/dismiss`; **zero changes to the chat loop or the confirm gate**. Computed **on demand** (60s poll + window focus), not a background loop — Phase 1 has no push channel until Tauri. Two gatherers: Calendar (deterministic; `prep` ≤45 min out, `deadline` all-day today/tomorrow) and commitments (one Claude call extracts `{what, who, by_when, done}` over the last 7 days of `daily/*.md`, **cached on a signature of names+mtimes+sizes** so it fires a few times a day, not per poll; `ensure_budget()` first, **skipped when the kill-switch is tripped**, usage recorded, exempt from the 5/min *request* cap). **Two safety invariants (do not relax):** the extraction call binds **no `tools=`** — structurally it can only return JSON, never act (asserted in a test) — and a nudge's `action` is a **prefill string that never auto-runs**, so anything that sends/creates still routes through the existing confirm gate. Sources are **trusted-only** (own Calendar + own notes); inbound-message reading is Part 3. Dismiss/snooze persist to gitignored `backend/.zenith/proactive.json` (atomic tmp→`os.replace`, corrupt → fresh) keyed by a stable `kind:slug:shorthash` id, so suppression survives recomputes while a materially-changed subject re-surfaces; orphaned/expired entries are pruned; a `done` commitment auto-clears. Every gatherer is best-effort — one throwing contributes nothing, and `GET /proactive` never 500s. HUD: `NudgeStack` + `NudgeCard` reuse StatusCard's notched shell via a new shared `cardShell.tsx` (`Corner`/`TONES` extracted; StatusCard renders byte-identically), so all three skins auto-theme with **zero per-skin work**; an **active confirm card always renders above the nudges**. Spec+plan `docs/superpowers/{specs,plans}/2026-07-09-proactivity*.md`. **+22 tests (221 backend green), tsc clean; live-verified across Arc/Ghost/Amethyst** with a real Claude extraction (open commitment surfaced · a logged-as-done one auto-cleared · a "just an idea" line correctly ignored · dismiss + snooze persisted across polls · the action button prefilled the CC without sending · confirm-card-on-top confirmed). Next: M7 Part 3 (message triage). v2.5 (App Launcher, SHIPPED 2026-07-09, `feat/app-launcher`): **open apps/files/folders/sites by name** as 2 tools on the EXISTING loop (`open_app`/`list_apps`), zero route changes. **Whitelist only** — `backend/app_launcher.py` resolves a NAME against the gitignored `backend/apps.json` (read fresh each call, `ZENITH_APPS_PATH` overrides; `apps.example.json` is the committed template + fallback); it never executes a caller-supplied path or command, and there is no shell escape hatch. Stdlib matching (normalize → exact name/alias → substring → `difflib` fuzzy) **refuses and lists options rather than guessing**. Five target kinds — `url`/`path` (ShellExecute; `.lnk` shortcuts are the robust way to open an installed desktop app)/`protocol`/`command` (`shutil.which`)/`uwp` (Store apps via `explorer.exe shell:AppsFolder\<AppUserModelID>`, found with `Get-StartApps`). New **`GATE_IF_UNTRUSTED`** set: `open_app` runs immediately normally, but becomes a confirm-gated pending action when the same turn already pulled untrusted `<external-content>` into context — a prompt-injected "open X" in an email can't auto-launch. It reuses `run_loop`'s existing `saw_untrusted` flag, so the gate itself is untouched (`open_app` is NOT in `ACTION_TOOLS`, NOT in `UNTRUSTED_TOOLS`). `open_app` logs to the Activity Log as type `note`; `list_apps` is unlogged. Explicitly out of scope: Computer Use / screenshots / clicking / typing, arbitrary commands or shell access, closing-killing apps, window management. Setup `SETUP-APPS.md`; spec+plan `docs/superpowers/{specs,plans}/2026-07-08-app-launcher*.md`. **+34 tests (197 fast suite green); live-verified across all five target kinds.** Also ran `/graphify` over the codebase (backend+frontend+docs → 1257 nodes / 1836 edges / 112 communities; outputs in the gitignored `graphify-out/`). Next: M7 (proactivity + WhatsApp triage). v2.4 (Notion integration + two app fixes, SHIPPED to main 2026-07-05, `feat/notion-integration` c9770ac→b8a63bb): **Notion as 18 Claude tools** on the EXISTING loop + confirm gate (direct Notion REST API, NOT MCP — same M3/M4 pattern, zero route/gate changes). Reads (fenced as `<external-content>`) list/search/read pages + query/describe DBs + read comments; gated writes create page (rich blocks: headings/checklists/paragraphs) + DB row, append/update/delete page blocks (find a line by text), update/archive (soft-delete → trash) pages, create/alter DB structure, add comment. New `backend/notion_service.py` + `backend/test_notion.py`. **Migrated to Notion's 2025-09-03 data-source model** so multi-data-source DBs work (the old 2022-06-28 API 400'd) — DB access routed through `_db_schema`/`_db_query`/`_db_parent` + `_data_source_id`; live QA surfaced 3 undocumented quirks fixed empirically: `/search` filters by `"data_source"` not `"database"`, column edits must `PATCH /data_sources/{id}` (DB-level PATCH silently no-ops), and search/list return *data-source* ids while URL ids are *database* ids (so `_data_source_id` resolves either, databases-first with a 404 fallback). Auth = internal-integration `NOTION_API_KEY`; pages/DBs must be **shared with the integration** in Notion or it sees nothing; comment tools 403 until the token's Read/Insert-comments capability is enabled. Setup `SETUP-NOTION.md`. **All 18 tools live-verified against the real workspace; 161 backend tests green, tsc clean.** Two ride-along app fixes: HUD **speaks confirmed-action results** (`resolvePending` captures the `/chat/confirm` reply and speaks it — it only wrote it silently before, so a done action went unnoticed with the tab unfocused; mirrors the voice path); daily token kill-switch raised **300k→500k**, now env-overridable via `ZENITH_DAILY_TOKEN_BUDGET`. Next: M7 (proactivity + WhatsApp triage). v2.3 (v7 HUD redesign + 2D-canvas orb port, SHIPPED to main 2026-07-03, 89a409a→f428c96): the full v7 HUD reskin (IconNav, ⌘K CommandPalette, Memory/Notes/Clients views, MonthRuler, v7 boot screen, persisted reduced-motion toggle in `lib/prefs.ts`) + the **2D-`<canvas>` orb port** (`OrbCanvas.tsx` — one renderer, per-skin sphere/mesh/nebula via `--orb-mode`; the three.js/R3F stack UNINSTALLED, `OrbScene.tsx` deleted → no WebGL anywhere; orange-on-speak on Arc sanctioned + the "no rings" law retired) + the mock-fidelity constellation `AmbientBackground` behind opaque rails. Two small fixes rode the merge: CC no longer presses the viewport bottom on long replies (`4a38bd7`) + env-driven CORS allowlist (`ZENITH_ALLOWED_ORIGINS`, `89a409a`). Then a post-merge `/review` fixed 7 findings (`f428c96`): stuck-mic keyup (Space-release in a text field), confirm-card restore on a network-fail, voice-error mislabel, 3 canvas-perf wins (single `getComputedStyle`/frame, squared-distance links, batched EQ halo), stale comments — security sweep clean. 130 backend tests green, tsc clean, cross-skin live-verified. Next: M7 (proactivity + WhatsApp triage). v2.2 (M6 Part 2 — Copy Factory, SHIPPED 2026-06-27, merged to main): three output-only Copy Factory tools on the EXISTING loop + confirm gate (zero route/gate changes) — `draft_sequence` (coherent multi-stage email + Meta WABA-template WhatsApp journey), `draft_ad_copy` (Meta primary/headlines/creative-brief OR Google RSA), `draft_landing_copy` (hero→trust→benefits→FAQ→CTA), in EN/HI/Hinglish (Latin only); NOT in ACTION_TOOLS (never gated) / NOT in UNTRUSTED_TOOLS (the owner's own briefs are trusted); new `backend/copy_factory.py` is a thin brief-assembler (resolves the brief from `clients/<name>.md` or inline text + the owner's voice from `notes/voice.md` + recent client notes → a directive; the loop's Claude writes the finished copy, saved via the existing `save_note` → Drafts tab); `MAX_TOKENS` 1024→8192 (non-streaming-safe) so a full sequence isn't truncated; +9 tests (111 fast suite green); live-verified on the real Obsidian vault; nothing wired to Arkquen. v2.1 (M6 Part 1 — memory vault, SHIPPED 2026-06-26, merged to main 76dfad1): local Markdown/Obsidian vault as 4 tools on the existing loop — search_notes/read_note/list_notes (read, trusted) + save_note (local write, not gated); `vault_service.py` with strict path-safety; daily logs; `ZENITH_VAULT_PATH` points at the owner's real Obsidian vault; read-only HUD VaultView on the Drafts/Clients rail tabs; +18 tests, live-verified. v2.0 (M5 part 2 — polish & completeness, SHIPPED 2026-06-26, merged to main commit 4d68ff5): real usage/cost dashboard (`UsagePanel` — `rate_limiter.stats()` now splits input/output tokens → est. ₹/$ cost via Sonnet-4.6 pricing + a `killswitch` flag; themed progress bars + cost line + OK/Tripped chip across all 3 skins; replaces the inline gauges); Settings page (`SettingsView` — Appearance=live SkinPicker / read-only active config from `/health` / Connections / Security; `/health` now returns `version`=0.5.0 + a `config` block `{auth_enforced, debug_logs}`); README + `backend/.env.example` rewrite (20 vars, grouped by service); error/empty/loading audit (ActivityLog loading-vs-down + Retry, ConnectionsPanel live/offline chip + stale `demo` badge removed, CalendarPanel Retry — verified live in a real backend-down state + auto-recovery); +18 tests (`test_usage_cost` / `test_tool_router` / `test_confirm_flow`; full fast suite 82 green). Closes Milestone 5; M6 (memory vault + Copy Factory) is next. v1.9 (security hardening — SHIPPED 2026-06-26, live-verified): Milestone 5 part 1 — backend X-Zenith-Token gate on every route except / and /health (auth.py; fail-open + loud warning when unset, 401 when set) + one frontend apiFetch wrapper (lib/api.ts); prompt-injection guard (fence untrusted read-tool content as <external-content> + a system-prompt rule + a same-turn ⚠️ warning on the HUD confirm card AND Telegram buttons; gate still never auto-approves); secrets restricted at rest (secure_files.harden — icacls/chmod 600 on .env + tokens/ at boot); thread-safe rate limiter verified (+ concurrency tests); logs scrubbed behind ZENITH_DEBUG_LOGS; fixed a .gitignore inline-comment bug that left backend/tokens/ (live OAuth tokens) un-ignored (verified never committed); +23 tests; live-verified (auth 401/200, real /chat, HUD all-200); Telegram/Discord unaffected (in-process). (Settings page + usage/cost dashboard + README were the M5 part-2 work — completed in v2.0 above.) Plus a morning-briefing add-on (separate PR): world+India news headlines via free RSS (`news_service` + a `get_news` tool; `NEWS_FEEDS` in .env; fenced as untrusted). v1.8 (skins — SHIPPED 2026-06-25): a tokenized-CSS-var skin system keyed by `data-skin` — Arc (cyan default, pixel-identical) / Ghost (light paper + ink, centered-focus layout, ink-network orb) / Amethyst (violet, rounded-glass bento layout); switch from Settings (SkinPicker) via an emil blur-mask crossfade + no-flash load; built with the Impeccable plugin + taste/minimalist/emil (spec+plan under docs/superpowers/). Tasks 1–10 done as atomic commits on main (9e3b7b7..bc2a3f2); cross-skin QA passed (HUD+boot per skin, no WebGL leak on orb sphere↔network switch, reduced-motion honored). v1.7 (voice → GPU): Kokoro TTS + Whisper STT moved onto the RTX 5060 (cu128 torch; whisper medium/cuda/float16 + cuDNN/cuBLAS), paragraph TTS 17.6s→~1.3s and STT ~1.1s with the CPU freed, plus boot-time inference warmups that kill the first-call cuDNN cold-start. v1.6 (Kokoro default): Kokoro became the local/offline TTS engine (edge-tts the one-flag fallback) on the Python-3.11 backend venv. v1.5 (orb + voice): orb rebuilt as a WebGL particle sphere (react-three-fiber + Bloom, audio-reactive core; supersedes the reactive-mesh orb + queued mesh-refinement); STT defaults to English with the Hinglish path kept dormant behind the flag; recommended desktop config large-v3/cuda/float16 with the silent CPU-fallback made visible (the real cause of the ~20s lag); edge-tts stays default, Kokoro logged as a future offline option. v1.4 (HUD build pass): the v1.3 app-style HUD is now implemented — reactive connection-mesh orb, merged Command Center, ConnectionsPanel + ActivityLog (split from CommsPanel), QuickActions, FocusCard, ContextRail; standalone WaveformBar removed; empty-mic-clip voice fix. Orb/HUD visual redesign queued in TODO.md. v1.3 adds (prior session): app-style UI direction (connection-map orb, paginated chat + left rail, connections list, activity log, first-class confirm card, fake telemetry cut, de-Marvel'd); business context (Arkquen out / ShapeOdyssey); and the personally-useful "future" features pulled into Phase 1 — Copy Factory (M6), Memory vault (M6), Proactivity + WhatsApp triage (M7). True SaaS machinery (multi-user, billing, hosting/PWA, full business dashboard) stays Phase 2.*
